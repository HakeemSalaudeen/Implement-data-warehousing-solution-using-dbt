# dbt_poc:
#   target: dev
#   outputs:
#     dev:
#       type: redshift
#       # host: "{{ env_var('REDSHIFT_HOST') }}"
#       # user: "{{ env_var('REDSHIFT_USER') }}"
#       # password: "{{ env_var('REDSHIFT_PASSWORD') }}"
#       host : dbt-poc.ck74sbgav22w.eu-central-1.redshift.amazonaws.com
#       user : akym-dev
#       password : xxx
#       port: 5439
#       dbname: dev
#       schema: "dbt_analytics"  # The schema where dbt will build your table
#       threads: 4



# dbt_poc:
#   target: dev
#   outputs:
#     dev:
#       type: snowflake
#       account: "HIEMLZO-BD72310" # Use a hyphen between the Organization and Account
#       user: "HAKYMDEVV"
#       password: "MvW4q9HpPPcybt8"
#       role: "ACCOUNTADMIN"
#       warehouse: "DBT_TEST1"        # Use underscores to avoid SQL syntax errors
#       database: "DBT_TEST1"         # Hyphens are treated as subtraction operators in SQL
#       schema: "PUBLIC"          # The default schema for your models
#       threads: 4                    # Number of parallel tasks



# dbt_poc:
#   target: dev
#   outputs:
#     dev:
#       type: snowflake
#       # account: "HIEMLZO-BD72310"  # Use a hyphen between the Organization and Account
#       # user: "HAKYMDEVV"
#       # password: "MvW4q9HpPPcybt8"
#       # role: "ACCOUNTADMIN"
#       # warehouse: "TRANSFORMING"        # Use underscores to avoid SQL syntax errors
#       # database: "ANALYTICS"         # Hyphens are treated as subtraction operators in SQL
#       # schema: "PUBLIC"          # The default schema for your models
#       threads: 4  

dbt_poc:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}"
      user: "{{ env_var('SNOWFLAKE_USER') }}"
      password: "{{ env_var('SNOWFLAKE_PASSWORD') }}"
      role: "{{ env_var('SNOWFLAKE_ROLE', 'ACCOUNTADMIN') }}"
      warehouse: "{{ env_var('SNOWFLAKE_WAREHOUSE') }}"
      database: "{{ env_var('SNOWFLAKE_DATABASE') }}"
      schema: "{{ env_var('SNOWFLAKE_SCHEMA', 'PUBLIC') }}"
      threads: 4